---
phase: 01-landing-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - css/style.css
autonomous: true
requirements:
  - LAND-01
  - LAND-02
  - LAND-03
  - LAND-05
  - LAND-06
  - LAND-07
  - LAND-08
  - LAND-09
  - LAND-14
  - LAND-16

must_haves:
  truths:
    - "index.html opens in a browser and renders a dark page (background #0A0A0F) with 4 visible sections"
    - "Hero block shows headline 'Кто будет расти в 2026 году', meta line '24 марта · 19:00 МСК · Бесплатно', speaker credit 'Вадим Сорокин, основатель EGZ', and a CTA button"
    - "Program block shows a heading and exactly 5 program items"
    - "Pain-points block shows a heading and exactly 5 list items with the exact copy from CONTEXT.md (not rephrased)"
    - "Final block contains a countdown placeholder area and two CTA buttons"
    - "Page uses no frameworks or build tools — single index.html links style.css and uses Inter from Google Fonts CDN"
    - "Layout is mobile-first: base styles target narrow viewports, desktop overrides inside @media (min-width: 768px)"
    - "CSS custom properties declared on :root match UI-SPEC values exactly"
  artifacts:
    - path: "index.html"
      provides: "Full page HTML structure with 4 blocks, all copy, all component hooks"
      contains: "id=\"final-block\""
    - path: "css/style.css"
      provides: "Complete design system and component styles"
      contains: "--color-bg: #0A0A0F"
  key_links:
    - from: "index.html"
      to: "css/style.css"
      via: "<link rel=\"stylesheet\"> in <head>"
      pattern: "rel=\"stylesheet\".*css/style\\.css"
    - from: "index.html"
      to: "https://fonts.googleapis.com"
      via: "<link> for Inter font"
      pattern: "fonts\\.googleapis\\.com.*Inter"
    - from: "index.html"
      to: "js/main.js"
      via: "<script src> before </body>"
      pattern: "src=\"js/main\\.js\""
---

<objective>
Build the complete HTML structure and CSS design system for the landing page. This plan produces a fully styled static page with all 4 blocks, all copy, and all component patterns — ready for JavaScript to be wired in Plan 02.

Purpose: Establish the visual foundation. After this plan the page is readable, styled, and mobile-responsive — just without live countdown and interactive behaviors.
Output: `index.html` (all 4 blocks + copy) and `css/style.css` (complete design system).
</objective>

<execution_context>
@/Users/nikitapopko/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nikitapopko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-landing-page/01-CONTEXT.md
@.planning/phases/01-landing-page/01-RESEARCH.md
@.planning/phases/01-landing-page/01-UI-SPEC.md

<interfaces>
<!-- Complete design system from UI-SPEC. Executor uses these values verbatim — no deviation. -->

CSS custom properties (declare on :root):
```css
:root {
  --color-bg: #0A0A0F;
  --color-surface: #12121A;
  --color-accent: #7C3AED;
  --color-accent-glow: rgba(124, 58, 237, 0.35);
  --color-accent-alt: #A855F7;
  --color-text: #F0F0F8;
  --color-text-muted: #8888A8;
  --color-border: rgba(255,255,255,0.08);
  --space-xs: 4px;
  --space-sm: 8px;
  --space-md: 16px;
  --space-lg: 24px;
  --space-xl: 32px;
  --space-2xl: 48px;
  --space-3xl: 64px;
  --text-body: 16px;
  --text-label: 14px;
  --text-heading: 20px;
  --text-display: 36px; /* desktop: 52px via media query */
}
```

Background texture (on body):
```css
background:
  radial-gradient(ellipse at 20% 50%, rgba(124,58,237,0.08) 0%, transparent 60%),
  radial-gradient(ellipse at 80% 20%, rgba(168,85,247,0.06) 0%, transparent 50%),
  #0A0A0F;
```

Primary CTA button:
```css
.btn-primary {
  background: linear-gradient(135deg, var(--color-accent), var(--color-accent-alt));
  border: none;
  min-height: 48px;
  padding: var(--space-md) var(--space-xl);
  border-radius: 8px;
  color: #fff;
  font-weight: 700;
  cursor: pointer;
  transition: box-shadow 0.25s ease, transform 0.1s ease;
  width: 100%;
}
.btn-primary:hover {
  box-shadow: 0 0 24px var(--color-accent-glow), 0 4px 16px rgba(0,0,0,0.4);
}
.btn-primary:active { transform: scale(0.97); }
.btn-primary:focus-visible { outline: 2px solid var(--color-accent); outline-offset: 3px; }
```

Ghost (secondary) CTA button:
```css
.btn-ghost {
  background: transparent;
  border: 1px solid var(--color-border);
  min-height: 48px;
  padding: var(--space-md) var(--space-xl);
  border-radius: 8px;
  color: var(--color-text-muted);
  font-weight: 400;
  cursor: pointer;
  transition: border-color 0.25s ease;
  width: 100%;
}
.btn-ghost:hover { border-color: rgba(255,255,255,0.25); color: var(--color-text); }
```

Scroll-reveal classes (applied in CSS; JS adds .visible):
```css
.reveal {
  opacity: 0;
  transform: translateY(24px);
  transition: opacity 0.5s ease, transform 0.5s ease;
}
.reveal.visible { opacity: 1; transform: translateY(0); }
@media (prefers-reduced-motion: reduce) {
  .reveal { transform: none; transition: opacity 0.5s ease; }
}
```

Section heading accent underline:
```css
.section-heading::after {
  content: '';
  display: block;
  width: 40px;
  height: 4px;
  background: var(--color-accent);
  margin-top: var(--space-sm);
}
```

Countdown cell:
```css
.countdown-cell {
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-left: 2px solid var(--color-accent);
  border-radius: 8px;
  width: 64px;
  height: 72px;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: var(--space-xs);
}
.countdown-digit {
  font-size: 48px;
  font-weight: 700;
  font-variant-numeric: tabular-nums;
  line-height: 1;
  color: var(--color-text);
}
.countdown-label {
  font-size: var(--text-label);
  color: var(--color-text-muted);
  text-transform: uppercase;
}
@media (min-width: 768px) {
  .countdown-cell { width: 80px; height: 96px; }
  .countdown-digit { font-size: 64px; }
}
```

Avatar with glow:
```css
.avatar-wrap {
  position: relative;
  width: 120px;
  height: 120px;
  border-radius: 50%;
  overflow: hidden;
}
.avatar-wrap::after {
  content: '';
  position: absolute;
  inset: -4px;
  border-radius: 50%;
  background: radial-gradient(circle, var(--color-accent-glow) 0%, transparent 70%);
  pointer-events: none;
}
.avatar-wrap img { width: 100%; height: 100%; object-fit: cover; border-radius: 50%; }
.avatar-initials {
  width: 100%;
  height: 100%;
  display: none;
  align-items: center;
  justify-content: center;
  background: var(--color-surface);
  color: var(--color-text-muted);
  font-size: 24px;
  font-weight: 700;
  border-radius: 50%;
}
```

Meta badge:
```css
.meta-badge {
  display: inline-block;
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: 100px;
  padding: var(--space-xs) var(--space-md);
  font-size: var(--text-label);
  color: var(--color-text-muted);
}
```

Breakpoints:
- Base (mobile <480px): single column, full-width buttons, display 36px
- Mobile-large (480–767px): max-width 440px centered, display 40px
- Desktop (≥768px): max-width 720px centered, display 52px, hero avatar floated right

Typography font stack:
`font-family: Inter, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;`
Letter-spacing for display: `-0.02em`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build index.html — all 4 blocks with copy and component hooks</name>
  <files>index.html</files>
  <read_first>
    - .planning/phases/01-landing-page/01-UI-SPEC.md (copywriting contract, component inventory, interaction contract)
    - .planning/phases/01-landing-page/01-CONTEXT.md (locked copy: pain-point items verbatim, CTA hrefs, speaker credit)
    - .planning/PROJECT.md (5 program items, product description)
    - .planning/phases/01-landing-page/01-RESEARCH.md (image fallback pattern, deep link format, anti-patterns)
  </read_first>
  <action>
Create `index.html` as a single-file page. No frameworks, no npm, no build tools (LAND-14).

HEAD:
- `<meta charset="UTF-8">`
- `<meta name="viewport" content="width=device-width, initial-scale=1">`
- `<title>Кто будет расти в 2026 году — Бесплатный эфир EGZ Academy</title>`
- Google Fonts CDN link: `https://fonts.googleapis.com/css2?family=Inter:wght@400;700&display=swap`
- `<link rel="stylesheet" href="css/style.css">`

BODY structure — exactly 4 sections:

**Section 1 — Hero (`<section class="hero">`):**
- `<header>` with `<img src="assets/egz-logo.svg" alt="EGZ Academy" class="logo" onerror="this.style.display='none'">`
- Display headline (h1): `Кто будет расти в 2026 году` — class `section-display reveal`
- Subheadline (p): Claude generates a 1-sentence contrast hook about marketing income gap — class `hero-sub reveal`
- Meta line: `<span class="meta-badge">24 марта · 19:00 МСК · Бесплатно</span>` — class `reveal`
- Avatar: `<div class="avatar-wrap reveal"><img src="assets/vadim-photo.jpg" alt="Вадим Сорокин, основатель EGZ Academy" onerror="this.style.display='none'; this.nextElementSibling.style.display='flex';"><div class="avatar-initials">ВС</div></div>`
- Speaker credit: `<p class="speaker-credit reveal">Вадим Сорокин, основатель EGZ</p>`
- Primary CTA: `<button class="btn-primary hero-cta reveal" onclick="scrollToFinal()">Зарегистрироваться</button>`

**Section 2 — Program (`<section class="program" id="program">`):**
- `<h2 class="section-heading reveal">` — Claude generates heading about «Что разберём на эфире»
- `<ol class="program-list">` with exactly 5 `<li class="program-item reveal">` items:
  1. Позиционирование: как маркетологу оторваться от конкурентов и стать выбором №1 в своей нише
  2. Лидген: системный поток входящих заявок без зависимости от сарафана
  3. Продажи: как закрывать клиентов на высокий чек без скидок и давления
  4. Удержание клиентов: LTV и повторные продажи — из клиента в партнёра
  5. CRM и воронка: автоматизировать рутину и масштабировать без найма
- Mid CTA: `<button class="btn-primary reveal" onclick="scrollToFinal()">Зарегистрироваться</button>`

**Section 3 — Pain points (`<section class="pain-points" id="pain-points">`):**
- `<h2 class="section-heading reveal">` — Claude generates heading about «Кому стоит прийти»
- `<ul class="pain-list">` with exactly 5 `<li class="pain-item reveal">` items — use EXACT copy from CONTEXT.md (do NOT rephrase):
  1. Много знаешь, но мало зарабатываешь
  2. Нет команды или команда совсем маленькая
  3. Нет сильного оффера и сильной упаковки
  4. Генерируешь меньше 2-3 заявок в месяц на свои услуги
  5. Тебя используют как руки а не как стратега
- CTA: `<button class="btn-primary reveal" onclick="scrollToFinal()">Зарегистрироваться</button>`

**Section 4 — Final (`<section class="final-block" id="final-block">`):**
- Heading (h2): Claude generates urgency/value hook — class `section-heading reveal`
- Countdown row: `<div class="countdown-row reveal">` with 4 `.countdown-cell` divs, each containing `<span class="countdown-digit" id="cnt-{days|hours|minutes|seconds}">00</span>` and `<span class="countdown-label">{ДНИ|ЧАС|МИН|СЕК}</span>`
- Static MSK tag: `<span class="msk-tag">МСК</span>` adjacent to countdown row (NOT inside cells)
- Post-deadline banner: `<div class="post-deadline" style="display:none"><p>Эфир уже идёт!</p></div>`
- Primary CTA: `<a href="https://t.me/<botname>?start=landing" class="btn-primary reveal">Зарегистрироваться в боте</a>` — add HTML comment `<!-- TODO Phase 3: replace <botname> with real bot username -->`
- Secondary CTA: `<a href="https://t.me/vadimsorokin_egz" class="btn-ghost reveal">Задать вопрос</a>`

**Sticky CTA bar** (outside sections, before `</body>`):
`<div class="sticky-cta"><a href="https://t.me/<botname>?start=landing" class="btn-primary sticky-btn">Зарегистрироваться</a></div>`

**noscript fallback:**
`<noscript>Включите JavaScript, чтобы таймер работал корректно.</noscript>`

Before `</body>`: `<script src="js/main.js"></script>`

Tone: дружеский, разговорный, на «ты» — как пост в Telegram-канале Вадима. No corporate language.
  </action>
  <verify>
    <automated>grep -c "id=\"final-block\"" "/Users/nikitapopko/Downloads/Telegram Desktop/Agent Teams/index.html" && grep -c "pain-item" "/Users/nikitapopko/Downloads/Telegram Desktop/Agent Teams/index.html" && grep -c "program-item" "/Users/nikitapopko/Downloads/Telegram Desktop/Agent Teams/index.html" && grep -c "botname" "/Users/nikitapopko/Downloads/Telegram Desktop/Agent Teams/index.html" && grep -c "vadimsorokin_egz" "/Users/nikitapopko/Downloads/Telegram Desktop/Agent Teams/index.html"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "id=\"final-block\"" index.html` returns 1
    - `grep -c "class=\"pain-item reveal\"" index.html` returns 5
    - `grep -c "class=\"program-item reveal\"" index.html` returns 5
    - `grep "href" index.html | grep "botname"` matches the deep link placeholder
    - `grep "vadimsorokin_egz" index.html` returns a line with the secondary CTA href
    - `grep "fonts.googleapis.com" index.html` returns a line with Google Fonts link for Inter
    - `grep "css/style.css" index.html` returns 1
    - `grep "js/main.js" index.html` returns 1
    - `grep "meta name=\"viewport\"" index.html` returns 1
    - `grep "avatar-initials" index.html` returns the initials block with text «ВС»
    - Pain-point items contain exact strings: «Много знаешь, но мало зарабатываешь», «Нет команды или команда совсем маленькая», «Нет сильного оффера и сильной упаковки», «Генерируешь меньше 2-3 заявок в месяц», «Тебя используют как руки а не как стратега»
    - `grep -i "react\|vue\|angular\|webpack\|vite\|npm" index.html` returns 0 matches
  </acceptance_criteria>
  <done>index.html exists with all 4 blocks, exact pain-point copy, deep link placeholder with TODO comment, secondary CTA to vadimsorokin_egz, Inter font via CDN, links to css/style.css and js/main.js.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build css/style.css — complete design system and all component styles</name>
  <files>css/style.css</files>
  <read_first>
    - .planning/phases/01-landing-page/01-UI-SPEC.md (full design system: colors, spacing scale, typography, component inventory, breakpoints, interaction contract)
    - .planning/phases/01-landing-page/01-RESEARCH.md (CSS patterns for glow, sticky CTA, scroll-reveal, anti-patterns to avoid)
    - index.html (class names and structure to target)
  </read_first>
  <action>
Create `css/style.css`. Mobile-first (base styles = mobile, desktop overrides in `@media (min-width: 768px)`).

**Section 1 — CSS custom properties on :root:**
Declare all tokens from UI-SPEC exactly as specified in the plan interfaces block above. Include all --color-*, --space-*, --text-* variables.

**Section 2 — Reset and base:**
```css
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
html { scroll-behavior: smooth; }
body {
  font-family: Inter, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  font-size: var(--text-body);
  color: var(--color-text);
  line-height: 1.6;
  background:
    radial-gradient(ellipse at 20% 50%, rgba(124,58,237,0.08) 0%, transparent 60%),
    radial-gradient(ellipse at 80% 20%, rgba(168,85,247,0.06) 0%, transparent 50%),
    var(--color-bg);
  min-height: 100vh;
}
```

**Section 3 — Layout container:**
Each section uses `.container` inner div: `max-width: 100%; padding: 0 var(--space-md);`
Desktop override: `max-width: 720px; margin: 0 auto; padding: 0 var(--space-lg);`

**Section 4 — Typography:**
- `.section-display`: `font-size: 36px; font-weight: 700; line-height: 1.15; letter-spacing: -0.02em; color: var(--color-text);`
  Desktop: `font-size: 52px;`
- `.section-heading`: `font-size: var(--text-heading); font-weight: 700; line-height: 1.3; color: var(--color-text);` + `::after` accent stripe (4px × 40px, `background: var(--color-accent)`, `margin-top: var(--space-sm)`)
- `.hero-sub`: `font-size: var(--text-body); color: var(--color-text-muted); margin-top: var(--space-md);`
- `.speaker-credit`: `font-size: var(--text-label); color: var(--color-text-muted); margin-top: var(--space-sm);`
- `.meta-badge`: pill badge as per interfaces block
- `.msk-tag`: `font-size: var(--text-label); color: var(--color-text-muted); font-weight: 700; margin-left: var(--space-sm); vertical-align: middle;`

**Section 5 — Buttons:**
Full `.btn-primary` and `.btn-ghost` CSS from interfaces block. Both `width: 100%` on mobile; desktop: `width: auto; min-width: 220px;`

**Section 6 — Hero section:**
Padding: `var(--space-3xl) 0 var(--space-2xl)`. On desktop: two-column flex (text left, avatar right).
Avatar wrap and avatar-initials CSS from interfaces block.
Logo: `max-height: 32px; margin-bottom: var(--space-xl);`

**Section 7 — Program section:**
`padding: var(--space-2xl) 0;`
`.program-list`: `list-style: none; display: flex; flex-direction: column; gap: var(--space-md); margin: var(--space-xl) 0;`
`.program-item`: `display: flex; gap: var(--space-md); align-items: flex-start; padding: var(--space-md); background: var(--color-surface); border: 1px solid var(--color-border); border-radius: 8px; font-size: var(--text-body); color: var(--color-text);`
Program item numbers: CSS counter — `counter-increment: program; ::before { content: counter(program); color: var(--color-accent); font-weight: 700; font-size: var(--text-heading); min-width: 24px; }`
`ol.program-list { counter-reset: program; }`

**Section 8 — Pain-points section:**
`padding: var(--space-2xl) 0;`
`.pain-list`: same structure as program-list but `list-style: none;`
`.pain-item`: `display: flex; gap: var(--space-md); align-items: flex-start; padding: var(--space-md); background: var(--color-surface); border: 1px solid var(--color-border); border-radius: 8px;`
Pain item bullet: `::before { content: '→'; color: var(--color-accent); font-weight: 700; }`

**Section 9 — Final block:**
`padding: var(--space-3xl) 0; text-align: center;`
`.countdown-row`: `display: flex; gap: var(--space-md); justify-content: center; margin: var(--space-xl) 0 var(--space-sm);`
Countdown cell and digit CSS from interfaces block.
`.post-deadline p`: `font-size: var(--text-heading); font-weight: 700; color: var(--color-accent); margin: var(--space-xl) 0;`
CTA stack in final: `display: flex; flex-direction: column; gap: var(--space-md); margin-top: var(--space-xl);`
Desktop final CTA stack: `flex-direction: row; justify-content: center;`

**Section 10 — Sticky CTA bar:**
Full CSS from interfaces block. `.sticky-cta`: position fixed, bottom 0, full width, gradient background, translateY(100%), z-index 100.
`.sticky-cta--visible`: translateY(0)
`.sticky-btn`: same gradient as `.btn-primary` but: `max-width: 400px; margin: 0 auto; display: block;`

**Section 11 — Scroll-reveal:**
`.reveal` and `.reveal.visible` CSS and `prefers-reduced-motion` media query from interfaces block.
Stagger: `.reveal:nth-child(2) { transition-delay: 0.1s; } .reveal:nth-child(3) { transition-delay: 0.2s; }` etc up to nth-child(5).

**Section 12 — Desktop media query (`@media (min-width: 768px)`):**
- body container max-width 720px centered
- display size 52px
- hero layout: flex row, avatar right
- countdown cell: 80px × 96px, digit 64px
- CTA buttons: width auto, min-width 220px
- program/pain-point sections may go 2-column grid if content allows

IMPORTANT: Do NOT use `backdrop-filter: blur()` on any critical UI element (Telegram WebView compatibility — see RESEARCH.md Pitfall 3). Use `box-shadow` for glow effects instead.
IMPORTANT: Do NOT apply `transform` or `will-change` to `body` (would break sticky CTA z-index stacking context — see RESEARCH.md Pitfall 2).
  </action>
  <verify>
    <automated>grep -c "\-\-color-bg: #0A0A0F" "/Users/nikitapopko/Downloads/Telegram Desktop/Agent Teams/css/style.css" && grep -c "\-\-color-accent: #7C3AED" "/Users/nikitapopko/Downloads/Telegram Desktop/Agent Teams/css/style.css" && grep -c "min-width: 768px" "/Users/nikitapopko/Downloads/Telegram Desktop/Agent Teams/css/style.css" && grep -c "sticky-cta" "/Users/nikitapopko/Downloads/Telegram Desktop/Agent Teams/css/style.css" && grep -c "translateY(100%)" "/Users/nikitapopko/Downloads/Telegram Desktop/Agent Teams/css/style.css"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "\-\-color-bg: #0A0A0F" css/style.css` returns a match
    - `grep "\-\-color-accent: #7C3AED" css/style.css` returns a match
    - `grep "\-\-color-surface: #12121A" css/style.css` returns a match
    - `grep "\-\-color-text: #F0F0F8" css/style.css` returns a match
    - `grep "min-width: 768px" css/style.css` returns at least 1 match (desktop breakpoint present)
    - `grep "translateY(100%)" css/style.css` returns 1 match (sticky CTA initial hidden state)
    - `grep "sticky-cta--visible" css/style.css` returns 1 match
    - `grep "backdrop-filter" css/style.css` returns 0 matches (not used — Telegram WebView compat)
    - `grep "will-change" css/style.css` returns 0 matches on body selector
    - `grep "prefers-reduced-motion" css/style.css` returns 1 match
    - `grep "font-variant-numeric: tabular-nums" css/style.css` returns 1 match
    - `grep "safe-area-inset-bottom" css/style.css` returns 1 match (iOS home indicator clearance on sticky CTA)
    - `grep "z-index: 100" css/style.css` returns 1 match (sticky CTA z-index)
  </acceptance_criteria>
  <done>css/style.css exists with all CSS custom properties matching UI-SPEC, all component styles for 4 sections, sticky CTA, scroll-reveal, mobile-first breakpoints, and no backdrop-filter usage.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Open index.html in a browser (file:// or python -m http.server) — page renders dark with #0A0A0F background
2. Count section blocks: hero, program (5 items), pain-points (5 items with exact copy), final block (countdown cells + 2 CTAs)
3. Resize to 375px width — layout is single-column, no horizontal overflow
4. Verify pain-point text matches CONTEXT.md exactly (no rephrasing)
5. Verify deep link href contains literal `<botname>` placeholder
6. `grep -i "react\|vue\|angular\|webpack\|vite" index.html` — returns 0
</verification>

<success_criteria>
- index.html and css/style.css exist in repo root and css/ directory
- Page renders 4 distinct sections with correct copy
- All 5 pain-point items use exact copy from CONTEXT.md
- CSS custom properties match UI-SPEC values (verifiable via grep)
- Mobile-first CSS with desktop breakpoint at 768px
- No frameworks, no build tools, no npm packages referenced
- Sticky CTA bar hidden by default (translateY 100%), shown by CSS class
- All design system tokens present on :root
</success_criteria>

<output>
After completion, create `.planning/phases/01-landing-page/01-01-SUMMARY.md` following the summary template.
</output>
