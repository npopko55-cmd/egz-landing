---
phase: 01-landing-page
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - js/main.js
  - .nojekyll
  - assets/vadim-photo.jpg
  - assets/egz-logo.svg
autonomous: true
requirements:
  - LAND-04
  - LAND-10
  - LAND-11
  - LAND-12
  - LAND-13
  - LAND-15

must_haves:
  truths:
    - "Countdown timer displays correct days/hours/minutes/seconds and counts down in real time"
    - "Countdown target is 2026-03-24T19:00:00+03:00 — explicit UTC offset guarantees correctness regardless of user device timezone"
    - "After deadline, countdown row is replaced by Эфир уже идёт! text; CTA button remains visible"
    - "Clicking hero, program, or pain-points CTA buttons smoothly scrolls to #final-block"
    - "Sticky CTA bar is hidden on page load and appears after hero CTA button scrolls out of viewport"
    - "All .reveal elements fade-in with translateY(0) when scrolled into view"
    - ".nojekyll file exists in repo root"
    - "assets/ directory contains placeholder files for vadim-photo.jpg and egz-logo.svg"
  artifacts:
    - path: "js/main.js"
      provides: "Countdown timer, IntersectionObserver reveal, sticky CTA logic, smooth scroll"
      contains: "2026-03-24T19:00:00+03:00"
    - path: ".nojekyll"
      provides: "Disables Jekyll on GitHub Pages"
    - path: "assets/vadim-photo.jpg"
      provides: "Placeholder image file"
    - path: "assets/egz-logo.svg"
      provides: "Placeholder SVG logo"
  key_links:
    - from: "js/main.js"
      to: "#cnt-days, #cnt-hours, #cnt-minutes, #cnt-seconds"
      via: "getElementById in tick() function"
      pattern: "getElementById\\('cnt-"
    - from: "js/main.js"
      to: ".hero .btn-primary"
      via: "IntersectionObserver stickyObserver"
      pattern: "querySelector.*hero.*btn-primary"
    - from: "js/main.js"
      to: ".sticky-cta"
      via: "classList.toggle sticky-cta--visible"
      pattern: "sticky-cta--visible"
    - from: "js/main.js"
      to: "#final-block"
      via: "scrollToFinal() function called by onclick in HTML"
      pattern: "scrollToFinal"
---

<objective>
Wire all JavaScript behaviors into the page and create GitHub Pages deployment infrastructure. After this plan the landing page is fully functional: live countdown, scroll-reveal animations, sticky CTA, smooth scroll — and ready to deploy to GitHub Pages.

Purpose: Complete the interactive layer of the landing page. All user-facing behaviors become operational.
Output: js/main.js (all interactive logic), .nojekyll (deployment gate), assets/ placeholder files.
</objective>

<execution_context>
@/Users/nikitapopko/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nikitapopko/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-landing-page/01-RESEARCH.md
@.planning/phases/01-landing-page/01-UI-SPEC.md
@.planning/phases/01-landing-page/01-CONTEXT.md
@.planning/phases/01-landing-page/01-01-SUMMARY.md

<interfaces>
DOM hooks that js/main.js targets (created in Plan 01):

Countdown digit elements (updated by tick()):
  id="cnt-days"       -- days remaining
  id="cnt-hours"      -- hours remaining
  id="cnt-minutes"    -- minutes remaining
  id="cnt-seconds"    -- seconds remaining

Countdown row (hidden on deadline):
  class="countdown-row"   -- the entire row, display:none after deadline

Post-deadline banner (shown on deadline):
  class="post-deadline"   -- initially display:none; set display:block after deadline

Scroll-reveal elements:
  class="reveal"           -- add class "visible" when isIntersecting (fires once)

Hero CTA button (triggers sticky observer):
  .hero .btn-primary       -- when this scrolls out of viewport, show sticky CTA

Sticky CTA bar:
  class="sticky-cta"      -- toggle class "sticky-cta--visible" via IntersectionObserver

Smooth scroll target:
  id="final-block"        -- scrollIntoView({ behavior: 'smooth' }) destination

scrollToFinal() function -- called by onclick on hero, program, pain-points CTAs.
Must be defined on window (global scope) because HTML uses onclick="scrollToFinal()".

Countdown target:
  const TARGET = new Date('2026-03-24T19:00:00+03:00');
  +03:00 = Moscow Standard Time. Russia does not observe DST (abolished 2014).
  This exact string guarantees the same countdown for all users regardless of device timezone.

Two-digit padding:
  const pad = n => String(n).padStart(2, '0');
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create js/main.js with countdown, scroll-reveal, sticky CTA, and smooth scroll</name>
  <files>js/main.js</files>
  <read_first>
    - index.html (exact IDs and class names, onclick attributes confirming scrollToFinal is called from onclick)
    - .planning/phases/01-landing-page/01-RESEARCH.md (patterns: countdown code, IntersectionObserver reveal, sticky CTA observer, anti-patterns section)
    - .planning/phases/01-landing-page/01-UI-SPEC.md (interaction contract: thresholds, transition values, sticky CTA behavior spec)
  </read_first>
  <action>
Create js/main.js. Vanilla ES6+. No imports, no npm, no build tools.

Structure: scrollToFinal and countdown at top level (no DOM dependency at definition time). Reveal observer and sticky observer inside DOMContentLoaded listener.

BLOCK 1 -- Smooth scroll (must be on window for onclick="" handlers in HTML):

  function scrollToFinal() {
    const finalBlock = document.getElementById('final-block');
    if (finalBlock) finalBlock.scrollIntoView({ behavior: 'smooth' });
  }
  window.scrollToFinal = scrollToFinal;

BLOCK 2 -- Countdown timer (LAND-11, LAND-09):

  const TARGET = new Date('2026-03-24T19:00:00+03:00');
  // +03:00 hard-codes Moscow Standard Time.
  // DO NOT remove this offset -- without it Date() parses as local device time.
  const pad = n => String(n).padStart(2, '0');

  function showPostDeadlineState() {
    clearInterval(timer);
    const row = document.querySelector('.countdown-row');
    const banner = document.querySelector('.post-deadline');
    if (row) row.style.display = 'none';
    if (banner) banner.style.display = 'block';
    // CTA button intentionally remains visible after deadline
  }

  function tick() {
    const diff = TARGET.getTime() - Date.now();
    if (diff <= 0) { showPostDeadlineState(); return; }
    const days    = Math.floor(diff / 86400000);
    const hours   = Math.floor((diff % 86400000) / 3600000);
    const minutes = Math.floor((diff % 3600000) / 60000);
    const seconds = Math.floor((diff % 60000) / 1000);
    const dEl = document.getElementById('cnt-days');
    const hEl = document.getElementById('cnt-hours');
    const mEl = document.getElementById('cnt-minutes');
    const sEl = document.getElementById('cnt-seconds');
    if (dEl) dEl.textContent = pad(days);
    if (hEl) hEl.textContent = pad(hours);
    if (mEl) mEl.textContent = pad(minutes);
    if (sEl) sEl.textContent = pad(seconds);
  }

  tick(); // run immediately -- avoids 1-second blank on page load
  const timer = setInterval(tick, 1000);

BLOCK 3 -- DOM-dependent observers inside DOMContentLoaded:

  document.addEventListener('DOMContentLoaded', () => {

    // Scroll-reveal (LAND-04): IntersectionObserver threshold 0.15, fires once per element
    const revealObserver = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('visible');
          revealObserver.unobserve(entry.target);
        }
      });
    }, { threshold: 0.15 });
    document.querySelectorAll('.reveal').forEach(el => revealObserver.observe(el));

    // Sticky CTA (LAND-10): watch hero primary CTA; show sticky bar when hero CTA leaves viewport
    const heroBtn = document.querySelector('.hero .btn-primary');
    const stickyCta = document.querySelector('.sticky-cta');
    if (heroBtn && stickyCta) {
      const stickyObserver = new IntersectionObserver((entries) => {
        stickyCta.classList.toggle('sticky-cta--visible', !entries[0].isIntersecting);
      }, { threshold: 0 });
      stickyObserver.observe(heroBtn);
    }

  });

CRITICAL constraints (from RESEARCH.md anti-patterns):
- Use setInterval(tick, 1000) NOT requestAnimationFrame for countdown
- Use IntersectionObserver NOT scroll event listener for reveal and sticky
- Target time MUST have +03:00 suffix -- do not change it
  </action>
  <verify>
    <automated>grep -c "2026-03-24T19:00:00+03:00" "/Users/nikitapopko/Downloads/Telegram Desktop/Agent Teams/js/main.js" && grep -c "scrollToFinal" "/Users/nikitapopko/Downloads/Telegram Desktop/Agent Teams/js/main.js" && grep -c "sticky-cta--visible" "/Users/nikitapopko/Downloads/Telegram Desktop/Agent Teams/js/main.js" && grep -c "IntersectionObserver" "/Users/nikitapopko/Downloads/Telegram Desktop/Agent Teams/js/main.js"</automated>
  </verify>
  <acceptance_criteria>
    - grep "2026-03-24T19:00:00+03:00" js/main.js returns 1 match (exact timezone-aware ISO string)
    - grep "scrollToFinal" js/main.js returns at least 2 matches (definition + window assignment)
    - grep "window.scrollToFinal" js/main.js returns 1 match (global exposure for onclick handlers)
    - grep "sticky-cta--visible" js/main.js returns 1 match
    - grep "IntersectionObserver" js/main.js returns at least 2 matches (reveal observer + sticky observer)
    - grep "setInterval" js/main.js returns 1 match
    - grep "requestAnimationFrame" js/main.js returns 0 matches
    - grep "scroll.*addEventListener\|addEventListener.*scroll" js/main.js returns 0 matches (no scroll event listeners)
    - node -e "const d = new Date('2026-03-24T19:00:00+03:00'); console.log(d.toISOString())" outputs 2026-03-24T16:00:00.000Z (16:00 UTC = 19:00 MSK)
    - grep "showPostDeadlineState" js/main.js returns at least 2 matches (definition + call)
    - grep "getElementById\('cnt-days'\)" js/main.js returns 1 match
  </acceptance_criteria>
  <done>js/main.js exists with countdown targeting 2026-03-24T19:00:00+03:00, post-deadline state toggle, two IntersectionObserver instances (reveal + sticky), scrollToFinal on window, no scroll event listeners, no requestAnimationFrame.</done>
</task>

<task type="auto">
  <name>Task 2: Create .nojekyll and assets/ placeholder files for GitHub Pages deploy</name>
  <files>.nojekyll, assets/vadim-photo.jpg, assets/egz-logo.svg</files>
  <read_first>
    - .planning/phases/01-landing-page/01-RESEARCH.md (GitHub Pages section: .nojekyll purpose, assets/ structure)
    - index.html (verify src paths used: assets/vadim-photo.jpg and assets/egz-logo.svg)
  </read_first>
  <action>
Create three files:

1. .nojekyll in repo root -- EMPTY file (zero bytes). Its presence tells GitHub Pages to skip Jekyll processing and serve files as-is. Do not put any content in it.

2. assets/vadim-photo.jpg -- a minimal valid JPEG placeholder so the browser does not log 404 errors. Create a 1x1 pixel grey JPEG, OR create a plain text file named vadim-photo.jpg with content "PLACEHOLDER - replace with real photo". The img onerror fallback in index.html will show initials if the image fails to render as an actual image. Either approach is acceptable; the goal is to have the file present so GitHub Pages does not serve 404.

3. assets/egz-logo.svg -- a minimal valid SVG placeholder. Use this exact content:
   ```
   <svg xmlns="http://www.w3.org/2000/svg" width="120" height="32" viewBox="0 0 120 32">
     <text x="0" y="24" font-family="Inter, sans-serif" font-size="20" font-weight="700" fill="#F0F0F8">EGZ Academy</text>
   </svg>
   ```
   This renders as a white text logo on transparent background. It will look correct against the dark page background.

After creating files, verify the directory structure:
  .nojekyll       (root, empty)
  index.html      (root)
  css/style.css   (subdirectory)
  js/main.js      (subdirectory)
  assets/vadim-photo.jpg  (subdirectory)
  assets/egz-logo.svg     (subdirectory)

Note: The deep link placeholder https://t.me/botname?start=landing in index.html is intentional. Add a comment in the HTML (already done in Plan 01): TODO Phase 3: replace botname with real bot username. This is LAND-13 -- placeholder is the correct Phase 1 state.
  </action>
  <verify>
    <automated>ls -la "/Users/nikitapopko/Downloads/Telegram Desktop/Agent Teams/.nojekyll" && ls -la "/Users/nikitapopko/Downloads/Telegram Desktop/Agent Teams/assets/vadim-photo.jpg" && ls -la "/Users/nikitapopko/Downloads/Telegram Desktop/Agent Teams/assets/egz-logo.svg"</automated>
  </verify>
  <acceptance_criteria>
    - ls .nojekyll returns the file (exists in repo root)
    - ls assets/vadim-photo.jpg returns the file
    - ls assets/egz-logo.svg returns the file
    - cat assets/egz-logo.svg contains "svg" and "EGZ Academy"
    - wc -c .nojekyll returns 0 (empty file)
    - ls css/style.css returns the file (from Plan 01)
    - ls js/main.js returns the file (from Task 1 of this plan)
  </acceptance_criteria>
  <done>.nojekyll exists as empty file in repo root, assets/egz-logo.svg exists as valid SVG with EGZ Academy text, assets/vadim-photo.jpg placeholder exists. All 6 required files present: index.html, .nojekyll, css/style.css, js/main.js, assets/vadim-photo.jpg, assets/egz-logo.svg.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete landing page: 4-block dark futuristic HTML/CSS/JS page with live countdown, scroll-reveal animations, sticky CTA, smooth scroll. Ready to open in browser and verify all behaviors work before deploy.
  </what-built>
  <how-to-verify>
1. From the repo root, start a local server: python3 -m http.server 8080
2. Open http://localhost:8080 in a browser (or open index.html directly via file://)
3. Check each success criterion from ROADMAP.md Phase 1:
   a. Page loads with dark background (#0A0A0F). Hero headline, meta badge, avatar placeholder visible.
   b. Countdown timer is counting down. Digits update every second. "МСК" tag is visible next to the timer.
   c. Click any of the 3 CTA buttons (hero, program section, pain-points section) -- page smoothly scrolls to final block.
   d. Scroll down past the hero section -- sticky CTA bar slides in from the bottom.
   e. Pain-points section: verify exact copy -- "Много знаешь, но мало зарабатываешь", "Нет команды или команда совсем маленькая", "Нет сильного оффера и сильной упаковки", "Генерируешь меньше 2-3 заявок в месяц на свои услуги", "Тебя используют как руки а не как стратега"
   f. Final block: two CTAs present -- primary "Зарегистрироваться в боте" (links to t.me placeholder) and secondary "Задать вопрос" (links to t.me/vadimsorokin_egz)
   g. Resize browser to 375px width -- layout is single column, no horizontal overflow, all CTAs are full-width
   h. View page source -- confirm no React/Vue/Angular/webpack references
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Complete functional verification:
1. node -e "const d=new Date('2026-03-24T19:00:00+03:00'); console.log(d.toISOString())" -- must output 2026-03-24T16:00:00.000Z (confirms +03:00 = UTC+3)
2. grep -i "react\|vue\|angular\|webpack\|vite\|npm" index.html -- must return 0 matches (LAND-14)
3. ls .nojekyll -- file must exist (LAND-15)
4. grep "botname" index.html -- must show placeholder deep link (LAND-13 Phase 1 state)
5. Open in browser: dark page, countdown running, sticky CTA appears on scroll
</verification>

<success_criteria>
- js/main.js exists with countdown targeting 2026-03-24T19:00:00+03:00 (exact string, with +03:00)
- post-deadline state replaces countdown row with Эфир уже идёт!, CTA button remains
- scrollToFinal() is exposed on window for onclick handlers
- Two IntersectionObserver instances: one for .reveal (threshold 0.15, fires once), one for sticky CTA (threshold 0)
- No scroll event listeners anywhere in main.js
- .nojekyll exists as empty file in repo root
- assets/egz-logo.svg exists as valid SVG
- assets/vadim-photo.jpg placeholder exists
- Human checkpoint: all 8 browser verification steps pass
</success_criteria>

<output>
After completion, create .planning/phases/01-landing-page/01-02-SUMMARY.md following the summary template.
</output>
